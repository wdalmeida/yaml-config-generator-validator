{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "helpers:pinGitHubActionDigests"
  ],
  "enabledManagers": ["github-actions", "custom.regex", "npm"],
  "minimumReleaseAge": "7 days",
  "schedule": ["before 6am on monday"],
  "configMigration": true,
  "osvVulnerabilityAlerts": true,
  "lockFileMaintenance": {
    "description": "config:recommended does not enable this by default - without it, a transitive/indirect package-lock.json entry (not a direct package.json dependency) only ever gets refreshed as a side effect of some direct dependency's own bump, so a patched sub-dependency could sit unpicked-up indefinitely. This regenerates the whole lockfile within the existing semver ranges on the same weekly schedule as everything else, in its own single PR.",
    "enabled": true,
    "schedule": ["before 6am on monday"]
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "actionlint: ci.yml pins the download-actionlint.bash script to a commit SHA of rhysd/actionlint itself, then passes the actionlint version to download as a separate argument - keep both in lockstep. rhysd/actionlint's git tags are v-prefixed (e.g. v1.7.13) but the bare currentValue captured here (1.7.12, no v - the download script rejects a v-prefixed argument) isn't, so extractVersionTemplate strips the v from every tag Renovate compares against; without it, the digest lookup can't find an exact tag match for the bumped bare version and fails with 'Could not determine new digest'.",
      "managerFilePatterns": ["/^\\.github/workflows/.*\\.ya?ml$/"],
      "matchStrings": [
        "actionlint/(?<currentDigest>[0-9a-f]{40})/scripts/download-actionlint\\.bash\\)\\s+(?<currentValue>[0-9.]+)"
      ],
      "depNameTemplate": "rhysd/actionlint",
      "datasourceTemplate": "github-tags",
      "versioningTemplate": "semver",
      "extractVersionTemplate": "^v(?<version>.*)$"
    },
    {
      "customType": "regex",
      "description": "gitleaks CLI, downloaded and checksum-verified directly from GitHub Releases in ci.yml (not the gitleaks-action wrapper) via a GITLEAKS_VERSION env var - matching that one declaration keeps every other reference to the version in the same step (interpolated from the env var) in sync automatically.",
      "managerFilePatterns": ["/^\\.github/workflows/.*\\.ya?ml$/"],
      "matchStrings": [
        "GITLEAKS_VERSION:\\s*(?<currentValue>[0-9.]+)"
      ],
      "depNameTemplate": "gitleaks/gitleaks",
      "datasourceTemplate": "github-releases",
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "description": "zizmor, installed from PyPI with an exact pinned version in ci.yml.",
      "managerFilePatterns": ["/^\\.github/workflows/.*\\.ya?ml$/"],
      "matchStrings": [
        "pip install zizmor==(?<currentValue>[0-9.]+)"
      ],
      "depNameTemplate": "zizmor",
      "datasourceTemplate": "pypi",
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "description": "Syft, installed and run directly (not via anchore/sbom-action) in both supply-chain.yml and release.yml via a shared SYFT_VERSION env var - keep both files in sync.",
      "managerFilePatterns": ["/^\\.github/workflows/.*\\.ya?ml$/"],
      "matchStrings": [
        "SYFT_VERSION:\\s*(?<currentValue>[0-9.]+)"
      ],
      "depNameTemplate": "anchore/syft",
      "datasourceTemplate": "github-releases",
      "versioningTemplate": "semver"
    }
  ],
  "packageRules": [
    {
      "description": "Batch every uses:/container digest bump (including plain re-pins where only the SHA changed, not the version) into one weekly PR instead of one per action - this repo already reviews and merges them together.",
      "matchManagers": ["github-actions"],
      "groupName": "github actions"
    },
    {
      "description": "Automerge GitHub Action digest-only re-pins once CI passes: the pinned version comment doesn't change, GitHub just moved the tag to a new commit, so there's no functional difference to review. Requires the repo's 'Allow auto-merge' setting (on) - Renovate uses GitHub's native auto-merge, which still waits for every required status check.",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["digest"],
      "automerge": true
    },
    {
      "description": "Automerge the workflow tool-version group (actionlint, gitleaks, zizmor, Syft) once CI passes - each is checksum- or SHA-verified at install time regardless of what Renovate bumps it to, and ci.yml's own actionlint/gitleaks/zizmor jobs plus supply-chain.yml's sbom job would fail outright if a bump broke anything.",
      "matchManagers": ["custom.regex"],
      "groupName": "workflow tool versions",
      "automerge": true
    },
    {
      "description": "Batch every npm minor/patch bump into one weekly PR (react, vite, typescript, @codemirror/*, etc.) and automerge once CI passes (full lint/build/test/schema suite). Majors are deliberately excluded from both grouping and automerge - each gets its own PR for manual review, since a major bump is more likely to need actual code changes.",
      "matchManagers": ["npm"],
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "npm dependencies (minor/patch)",
      "automerge": true
    }
  ]
}
